version: '3.8'

services:
  analytics-app:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: rpl-analytics-app
    ports:
      - "3000:3000"
    environment:
      - NODE_ENV=production
      - PORT=3000
    restart: always
